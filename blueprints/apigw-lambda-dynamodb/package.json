{
  "name": "blueprint-apigw-lambda-dynamodb",
  "version": "1.0.0",
  "description": "IDP Blueprint: API Gateway + Lambda + DynamoDB",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "diff": "cdk diff",
    "lint": "eslint lib bin --ext .ts"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.252.0",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.252.0",
    "typescript": "^5.7.0"
  }
}
