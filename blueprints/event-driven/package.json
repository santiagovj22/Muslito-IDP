{
  "name": "blueprint-event-driven",
  "version": "1.0.0",
  "description": "IDP Blueprint: EventBridge + SQS + Lambda consumer",
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "diff": "cdk diff"
  },
  "dependencies": { "aws-cdk-lib": "^2.140.0", "constructs": "^10.3.0" },
  "devDependencies": { "@types/node": "^20.12.7", "aws-cdk": "^2.140.0", "typescript": "^5.4.5" }
}
